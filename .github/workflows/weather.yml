name: Weather

on:
  schedule:
    - cron: '50 * * * *'

jobs:
  weather:
    name: Run weather
    runs-on: ubuntu-latest
    steps:
      - name: Run weather
        uses: docker://circleci/python:3.7
        run: pipenv run python3 ./bots/weather.py
